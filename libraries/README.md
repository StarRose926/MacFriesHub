## What is this folder?
* It contains functions/libraries that MacFries Hub uses!

## What is the diffirence between "Internal" and "External"?
* Internal: Internal is loaded into the Game by ModuleScripts because of them needing a ModuleScript type envirement!
* External: External is loaded into the Script itself (into Memory), so it dosent need a ModuleScript
