-- BY: StarRose
--
-- Fallback for executors like "Solara" again :skull:
--
-- NOTE: Patched :( - does not work anymore

return function(part)
	return part.Anchored == false and part.ReceiveAge == 0
end
